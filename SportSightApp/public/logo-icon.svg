<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" fill="none">
  <defs>
    <linearGradient id="bg" x1="0" y1="0" x2="100" y2="100" gradientUnits="userSpaceOnUse">
      <stop offset="0%" stop-color="#2563eb"/>
      <stop offset="100%" stop-color="#7c3aed"/>
    </linearGradient>
  </defs>

  <!-- Background -->
  <rect width="100" height="100" rx="22" fill="url(#bg)"/>

  <!-- 2nd place (left) -->
  <rect x="15" y="45" width="20" height="40" rx="4" fill="white" opacity="0.65"/>

  <!-- 1st place (center) -->
  <rect x="40" y="28" width="20" height="57" rx="4" fill="white"/>

  <!-- 3rd place (right) -->
  <rect x="65" y="58" width="20" height="27" rx="4" fill="white" opacity="0.45"/>
</svg>
